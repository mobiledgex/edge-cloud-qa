apiVersion: apps/v1
kind: Deployment
metadata:
  name: crmtmocloud8
  labels:
    app: crmtmocloud8
spec:
  replicas: 1
  selector:
    matchLabels:
      app: crmtmocloud8
  template:
    metadata:
      labels:
        app: crmtmocloud8
    spec:
      containers:
      - name: crmtmocloud8
        image: registry.mobiledgex.net:5000/mobiledgex/edge-cloud:$AUTOMATION_DOCKERTAG
        imagePullPolicy: Always
        resources:
          requests:
            cpu: 10m
        command:
         - crmserver
        args:
         - "--apiAddr"
         - "0.0.0.0:50098"
         - "--cloudletKey"
         - "{\"operator_key\":{\"name\":\"tmus\"},\"name\":\"tmocloud-8\"}"
         - "--notifyAddrs"
         - "controller:37001"
         - "--tls"
         - "/root/tls/mex-server.crt"
         - "--platform"
         - "fakecloudlet"
         - "--d"
         - "api,notify,mexos"
      imagePullSecrets:
       - name: mexreg-secret
